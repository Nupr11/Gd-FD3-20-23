import styles from "./Footer.module.css";

export const Footer = ({ className }) => {
  return (
    <section className={className}>
      <h4 className={styles.FooterTitle}>Footer</h4>
    </section>
  );
};
